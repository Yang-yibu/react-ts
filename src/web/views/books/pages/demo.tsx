// import React from 'react';

import * as React from 'react';

const Hello = ({ name }) => <div>Hello, {name}</div>;

// const Hello: React.SFC<{ name: number }> = ({ name }) => (
//   <div>Hello,{name}</div>
// );

export default Hello;